spring:
  application:
    name: backend

  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:apartment_building_manager_system}?createDatabaseIfNotExist=true
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:123456}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  mail:
    host: ${MAIL_HOST:localhost}
    port: ${MAIL_PORT:1025}
    properties:
      mail:
        smtp:
          auth: false
          starttls:
            enable: false

  security:
    jwt:
      secret: ${JWT_SECRET:404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970}
      expiration: 3600000 # 1 hour
      refresh-token:
        expiration: 2592000000 # 30 days

cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:dnkghao4p}
  api-key: ${CLOUDINARY_API_KEY:399479765913834}
  api-secret: ${CLOUDINARY_API_SECRET:kG6ErLLWRvSFnvA9x0S2ln-2G7U}
